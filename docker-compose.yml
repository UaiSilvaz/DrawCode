version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: drawcode_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: drawcode
      POSTGRES_PASSWORD: drawcode_secret
      POSTGRES_DB: drawcode_db
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  adminer:
    image: adminer:latest
    container_name: drawcode_adminer
    restart: unless-stopped
    ports:
      - '8080:8080'
    depends_on:
      - postgres

volumes:
  postgres_data:
